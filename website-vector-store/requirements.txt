# Core pipeline runs on numpy + stdlib (hashing embedder + sqlite store).
numpy>=1.24
# Crawling (only needed for `build`/`update`, not for query with the hashing embedder):
requests>=2.31
beautifulsoup4>=4.12
# Optional: production embeddings via any OpenAI-compatible endpoint use `requests` (above).
