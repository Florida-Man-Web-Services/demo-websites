# Goal

Produce an exhaustive AI411 feature audit and gap list. AI411 = FMWS's public AI phone line + Gainesville directory + product funnel. You are the architect; lists from ALL angles; then we implement gaps one by one in later rounds with hierarchical agent teams. Budget for the whole effort: 360 rounds. This round: the lists and the ranked gap backlog only — no implementation.

# Verified ground truth (from repo + live cluster, 2026-09-21)

## Surfaces / hosts
- `ai411.floridamanweb.online` — public landing (demo-sites nginx `/ai411/`): hero, directory blurb, "Free demo website callback" form (POST `/api/onboarding/register`), personal-page opt-in pitch, "what happens next" section.
- `floridamanweb.online/<hash>/` — 261 generated demo sites (one HTML per business slug).
- `voice.flmanbiosci.net` — voice-agent FastAPI (Twilio voice/SMS webhooks + public APIs).
- `sites.floridamanweb.online` — site-tracker desk CRM behind Authentik (customers funnel).
- `mcp.flmanbiosci.net` — remote MCP (bearer).

## Agent modes (live `AGENT_MODE=auto`, resolves per-call via customers.resolve_mode)
- ai411 (public callers): tools = search_business_knowledge, lookup_business, search_events, summarize_event_categories, get_event, list_event_sources, search_activities, submit_event_broadcast, submit_notice_broadcast, list_recent_broadcasts, report_broadcast, delete_own_broadcast, get_question_of_the_day, answer_question_of_the_day, suggest_question_of_the_day, get_caller_people_profile, match_events_for_profile, express_event_interest, list_event_interest_matches, opt_in/out_personal_page, get_personal_page_status, send_demo_link_* + log_call_outcome + end_call (sales set), LIFECYCLE_TOOLS.
- onboarding (interview): get_customer_profile, save_onboarding_answer, finalize_requirements, queue_website_build, log_call_outcome, end_call.
- sales: send_demo_link_sms/email, log_call_outcome, end_call (outbound slug dial).
- owner_updates (paid/active_owner): lookup_business, get_site_outline, create_change_request, list/cancel/get_change_request, apply local, mark_request_shipped, open_site_update_pr, LIFECYCLE_TOOLS (request_account_step_up etc.).
- front_desk (business front desk persona; publication lookup).
- unified (legacy pin).

## MCP server tools (~60): callers CRUD/profile/forget/notes, qotd suite, people profile, event matching + interest, personal pages opt-in/out/status, change-request suite, siteedit/sitepr (with new assetgraph fail-closed audit), events/activities/broadcasts suite, account lifecycle (status, prepare_client_page(+removal), trusted phone add/remove, cancel_account_operation), lookup, pitch, call history, log outcome, knowledge, snapshot.

## HTTP API (voice-agent): /voice/inbound|outbound|turn|status, /sms/inbound, /api/onboarding/register|place-callback|customers, /api/sms/notify-updated, /api/billing/mark-paid, /api/owner/voice/enroll|forget, /api/personal-pages/opt-in|opt-out|status, /me/{slug} personal page, /clients/{slug}, /businesses/{slug}/ + /requests (hosted CMS render + intake), /audio/{key}.wav, /health.

## Data: customers.json funnel statuses (prospect→callback_queued→onboarding→requirements_ready→building→demo_ready/sales_ready→paid/active_owner→churned/do_not_call); call-log SQLite+CSV; builder-briefs/; customer-memory (Honcho or local); broadcasts store; website-vector-store (separate container, MCP tool).

## Ship pipeline: ChangeRequest → siteedit (deterministic) → sitepr PR → automerge (SITE_PR_AUTOMERGE=1) → CI (asset-graph audit guard) → image → kubectl roll. Owner auth: F1 CID trusted_phones (live), F2 voice enrollment endpoints exist (/api/owner/voice/enroll|forget). Stripe: payment link in sales prompt + mark-paid endpoint (manual). Twilio signature validation on. A2P/10DLC pending FMWS entity gate.

## Known soft spots (from ops history, verify)
- Stripe is link-in-prompt + manual mark-paid; no webhook automation.
- Builder step (6) is me/Hermes waves, not an automated agent service.
- Cluster API transiently unreachable right now (10.41.0.100 timeout) — live probes pending.
- Docs: ARCHITECTURE/PRODUCT_LOOP/API/OPS_CLUSTER/front-desk/hosted-business-cms exist; operator-facing runbooks may be stale (last verified 2026-08-11).

# Your task this round
1. Feature inventory from ALL angles: (a) public caller, (b) prospect/funnel, (c) paying owner, (d) operator (Noah), (e) builder agents, (f) compliance/abuse. For each: is the frontend present, the backend present, the process/guideline defined, the operator doc present?
2. Ranked gap backlog P0/P1/P2 with one-paragraph implementation sketches + acceptance checks.
3. An ordered verification checklist for the end state (what "AI411 delivers everything it promises" means, testable).
4. Flag anything where frontend exists but backend is missing, backend exists but frontend/process missing, or process exists but undocumented.
Output as structured lists I can dispatch against. Rounds are cheap (360) — be exhaustive, not terse.
